package com.nicholas.rutherford.track.your.shot.base.resources

/**
 * Created by Nicholas Rutherford, last edited on 2025-08-16
 *
 * Centralized object holding references to drawable and mipmap resources used across the app.
 * This helps maintain consistency and makes it easier to reference images programmatically.
 */
object DrawablesIds {

    /** Drawable representing a made basketball shot. */
    val basketballShotMade = R.drawable.ic_basketball_shot_made

    /** Drawable representing a camera icon. */
    val camera = R.drawable.ic_camera

    /** Drawable used for creating player UI elements. */
    val createPlayers = R.drawable.ic_create_players

    /** Mipmap representing an email icon. */
    val email = R.mipmap.ic_email

    /** Drawable used as a placeholder image. */
    val placeholder = R.drawable.ic_placeholder

    /** Drawable used as the splash screen icon. */
    val splash = R.drawable.ic_splash_icon

    /** Drawable representing basketball statistics. */
    val statisticBasketball = R.drawable.ic_statistic_basketball

    /** Drawable representing a basketball icon. */
    val sportsBasketball = R.drawable.ic_sports_basketball

    /** Mipmap used for the app's round launcher icon. */
    val launcherRound = R.mipmap.ic_launcher_round

    /** Mipmap used for the stage environment round launcher icon. */
    val launcherRoundStage = R.mipmap.ic_launcher_round_stage

    /** Mipmap used for the test environment round launcher icon. */
    val launcherRoundTest = R.mipmap.ic_launcher_round_test
}
